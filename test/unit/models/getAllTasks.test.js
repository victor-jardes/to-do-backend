// const { expect } = require('chai');
// const sinon = require('sinon');
// const connection = require('../../../models/connection');
// const getAllTaskModel = require('../../../models/getAllTaskModel');
// const { full } = require('../../mocks/tasksMock');

// minimal test for test mocha, but bug in mocha, implements test later
describe('test', () => {
  expect(10).to.be.equal(10);
});
